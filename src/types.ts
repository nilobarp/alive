export enum StateVariant {
  success = 'success',
  info = 'info',
  warning = 'warning',
  danger = 'danger'
}

export enum TypeVariant {
  default = 'default',
  primary = 'primary',
  secondary = 'secondary',
  accent = 'accent'
}

export enum ElementSize {
  small = 'small',
  default = 'default',
  large = 'large'
}
